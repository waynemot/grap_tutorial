<div class="jumbotron">
  <h1>Ruby Graph Tutorial</h1>
  <p class="lead">This sample app shows how to use the Microsoft Graph API to access a user's data from Ruby</p>
  <% if @user_name %>
    <h4>Welcome <%= @user_name %>!</h4>
    <p>Use the navigation bar at the top of the page to get started.</p>
  <% else %>
    <%=form_tag "/auth/microsoft_graph_auth" %>
      <%= submit_tag "Click here to sign in", method: :post, class: "btn btn-primary btn-large" %>
  <% end %>
</div>

